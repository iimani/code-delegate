You are working non-interactively in a git repository. Nobody will answer questions or approve steps: make reasonable decisions yourself and finish the task.

When you are done, the finished code must be in the working tree of the branch that is currently checked out (commit it there). Code left only on another branch or worktree does not count.

The task:
