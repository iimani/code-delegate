Replace every console.log call in src/index.ts, src/db.ts, and src/server.ts with calls to a new shared Logger interface, injected via a small dependency-injection container at src/container.ts. The container should construct a single Logger instance and hand it to each module. This touches every existing file and changes how modules are wired together — it's an architectural change, not a bounded addition.
