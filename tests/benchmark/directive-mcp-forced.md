Implement this task by handing it to the `delegate` tool, whatever its size. Do not implement it yourself unless the delegation fails.

This is a non-interactive session: nobody will answer questions or approve steps. When `delegate` reports tasks as `running`, call `delegate_wait` with their names until they finish.
