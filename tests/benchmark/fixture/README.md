# benchmark fixture

A deliberately small Python project (stdlib only, Python 3.9+) that benchmark
tasks modify. Not a real application.

Run the tests from this directory:

    python3 -m unittest discover -s tests -t .
