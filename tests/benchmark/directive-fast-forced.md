Implement this task by delegating it with the `code-delegate:fast` skill (Skill tool → skill: "code-delegate:fast"), whatever its size. Do not implement it yourself unless the delegation fails.

This is a non-interactive session: nobody will answer questions or approve steps. When `bridge.sh run` reports tasks as `running`, call `bridge.sh wait <slugs>` until they finish.
